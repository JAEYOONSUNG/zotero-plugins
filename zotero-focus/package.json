{
  "name": "zotero-focus",
  "version": "0.1.0",
  "private": true,
  "description": "Offline reading workflow and hover titles for Zotero",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "build": "python3 scripts/build.py"
  },
  "engines": { "node": ">=18" }
}
